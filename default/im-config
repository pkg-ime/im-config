# Deault im-config mode (see im-config(8))

# Always start best input method
IM_CONFIG_DEFAULT_MODE=auto
# Start best input method only if CJKV environment
#IM_CONFIG_DEFAULT_MODE=auto-cjkv

# Trace commands for debug
#set -x
