# vim: set sts=4 expandtab:
IM_CONFIG_SHORT="$(gettext "do not use any IM for input")"

IM_CONFIG_LONG="$(gettext "This forces not to use any IM for input.
This is the automatic configuration choice if no required IM packages are installed.")"

# This always return true
package_auto () {
:
}

# This always return true
package_menu () {
:
}

